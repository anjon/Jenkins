pipeline {
    agent any

    stages {

        stage('Building Docker Image') {
            steps {
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
        }
    }
}